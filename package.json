{
  "name": "mapper-service",
  "version": "1.0.0",
  "description": "Node.js server to dynamically map RunPod URLs to Nginx",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  }
}
